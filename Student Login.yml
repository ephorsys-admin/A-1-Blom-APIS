info:
  name: Student Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/student/login
  body:
    type: json
    data: |-
      {
          "mobile": "7735429894",
          "password": "samir@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
