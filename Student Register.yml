info:
  name: "Student Register "
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/student/register
  body:
    type: json
    data: |-
      {
        "fullName": "Samir kumar swain",
        "mobile": "7735429894",
        "email": "samir.ephorsys@gmail.com",
        "classNumber" : 8,
        "password": "samir@123",
        "gender": "Male",
        "acceptedTerms": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
