info:
  name: Verify-email
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/api/v1/student/verify-email-otp
  body:
    type: json
    data: |-
      {
        "email": "bisworanjan.ephorsys@gmail.com",
        "otp": "720888"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
