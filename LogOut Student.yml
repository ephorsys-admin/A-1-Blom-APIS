info:
  name: LogOut Student
  type: http
  seq: 7

http:
  method: POST
  url: https://a1-blom-education-backend.onrender.com/api/v1/student/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
