info:
  name: Resend-mail
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:8080/api/v1/student/resend-email-otp
  body:
    type: json
    data: |-
      {
        "email":"bisworanjan.ephorsys@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
