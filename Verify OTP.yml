info:
  name: Verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/student/verify-mobile-otp
  body:
    type: json
    data: |-
      {
        "mobile": "7064515926",
        "otp": "213866"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
