info:
  name: Verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: https://a1-blom-education-backend.onrender.com/api/v1/student/verify-mobile-otp
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
