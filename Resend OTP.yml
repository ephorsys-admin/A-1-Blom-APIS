info:
  name: Resend OTP
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/student/resend-mobile-otp
  body:
    type: json
    data: |-
      {
          "mobile": "6371287364"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
