info:
  name: Resend OTP
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/v1/student/resend-mobile-otp
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
